# Task - complete example matrix across REST, RPC, and streams

## Objective

Ensure every major implemented family has at least one discoverable example under [`examples/binapi2/fapi`](examples/binapi2/fapi).

## Work

- fill missing examples for newly implemented public REST, authenticated REST, websocket RPC, user stream, and Convert flows
- keep examples grouped and named consistently with existing example targets in [`examples/binapi2/fapi/CMakeLists.txt`](examples/binapi2/fapi/CMakeLists.txt)
- document any deliberately omitted live-trading flows that are unsafe to run casually
